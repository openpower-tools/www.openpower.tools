# www.openpower.tools

Source for <https://www.openpower.tools>, a community-driven support project for
OpenPOWER / Talos II firmware and software ports.

At the moment the site is a single index page. What this repository establishes
is the plumbing: Lit web components, a Rust/WebAssembly module, a GitHub Actions
build, and GitHub Pages hosting on a custom domain. Content comes next.

openpower.tools is an independent community project. It is not affiliated with
or endorsed by the OpenPOWER Foundation, IBM, or Raptor Computing Systems.

## Stack

- [Lit](https://lit.dev/) 3 web components written in TypeScript. Components use
  `static properties` and `declare`d fields rather than decorators, so the build
  does not depend on any decorator transform.
- Rust on a pinned nightly toolchain (`rust-toolchain.toml`), compiled to
  `wasm32-unknown-unknown` with [wasm-pack](https://github.com/wasm-bindgen/wasm-pack)
  and wasm-bindgen, then size-optimised with a pinned
  [binaryen](https://github.com/WebAssembly/binaryen) `wasm-opt` from npm. The
  crate lives in `wasm/`.
- [Vite](https://vite.dev/) 8 for the dev server and the production bundle. The
  wasm-bindgen glue refers to its `.wasm` file with
  `new URL('...', import.meta.url)`, which Vite turns into a hashed asset; no
  wasm plugin is needed.
- GitHub Actions builds and deploys to GitHub Pages
  (`.github/workflows/deploy.yml`).

## Layout

```
.github/workflows/deploy.yml   build on push/PR, deploy to Pages on main
.cargo/config.toml             warnings are errors for Rust
Cargo.toml                     workspace root (release profile: size, trim-paths)
rust-toolchain.toml            pinned nightly + wasm32-unknown-unknown + rustfmt/clippy
wasm/                          Rust crate compiled to WebAssembly
index.html                     the index page; mounts <op-home>
src/main.ts                    registers the custom elements
src/components/op-home.ts      the index page component
src/wasm.ts                    loads the wasm-pack output once, exposes typed info
src/wasm-pkg/                  generated by `npm run build:wasm` (not committed)
docs/research/                 research notes with sources (landscape, stack verification)
```

## Local development

Prerequisites: Node.js 22.12 or newer and rustup. rustup reads
`rust-toolchain.toml` and installs the pinned nightly toolchain, the wasm32
target, rustfmt and clippy on first use (`rustup toolchain install` does it
explicitly). wasm-pack and binaryen are npm devDependencies; wasm-pack downloads
the wasm-bindgen CLI matching `Cargo.lock` on first run.

```
npm ci
npm run build      # wasm-pack build, wasm-opt, tsc --noEmit, vite build  ->  dist/
npm run dev        # wasm build, then the Vite dev server
npm run preview    # serve dist/ locally
npm run hashes     # sha256 of the wasm, glue and dist/ files
```

Rust checks, identical to CI:

```
cargo fmt --all --check
cargo clippy --workspace --all-targets --target wasm32-unknown-unknown -- -D warnings
cargo test --workspace
```

## Reproducible builds

`dist/` is meant to be byte-identical wherever it is built. Everything that
feeds into it is pinned:

- the compiler: a dated nightly in `rust-toolchain.toml`;
- Rust dependencies: `Cargo.lock`; wasm-pack picks the matching wasm-bindgen CLI
  from it;
- wasm-pack, binaryen (`wasm-opt`), TypeScript, Vite and Lit: `package-lock.json`
  (binaryen is pinned exactly; wasm-pack's own wasm-opt is disabled in
  `wasm/Cargo.toml` so the npm one is the only optimiser);
- paths: the release profile sets the nightly-only `trim-paths = "all"` so
  `~/.cargo/registry` and `~/.rustup` paths do not end up in the binary.

Every CI run prints `npm run hashes` in its job summary. To check a build,
run `npm run build && npm run hashes` locally and compare.

## Deployment

Every push to `main` runs the workflow: the `build` job installs the pinned
toolchain with `rustup toolchain install`, runs the Rust checks and
`npm run build`, records the artifact hashes, uploads `dist/` as the Pages
artifact, and the `deploy` job publishes it to the `github-pages` environment.
Pull requests run the `build` job only.

Pages is configured in the repository settings rather than in files: the
publishing source is the workflow (`build_type: workflow`) and the custom domain
is `www.openpower.tools`. There is no `CNAME` file in the repository because
GitHub ignores it for workflow-published sites, and no `.nojekyll` because
Jekyll never runs on an uploaded artifact.

The same settings via the REST API:

```
gh api -X POST repos/openpower-tools/www.openpower.tools/pages -f build_type=workflow
gh api -X PUT  repos/openpower-tools/www.openpower.tools/pages -f cname=www.openpower.tools
gh api repos/openpower-tools/www.openpower.tools/pages/health
```

## DNS

`openpower.tools` is registered at Namecheap and uses Namecheap BasicDNS. The
canonical hostname is `www.openpower.tools`; GitHub Pages redirects the apex to
it automatically once both sets of records below exist.

| Type  | Host  | Value                          |
| ----- | ----- | ------------------------------ |
| CNAME | `www` | `openpower-tools.github.io.`   |
| A     | `@`   | `185.199.108.153`              |
| A     | `@`   | `185.199.109.153`              |
| A     | `@`   | `185.199.110.153`              |
| A     | `@`   | `185.199.111.153`              |
| AAAA  | `@`   | `2606:50c0:8000::153`          |
| AAAA  | `@`   | `2606:50c0:8001::153`          |
| AAAA  | `@`   | `2606:50c0:8002::153`          |
| AAAA  | `@`   | `2606:50c0:8003::153`          |

Notes:

- Remove Namecheap's default parking records (`@` URL redirect and `www` CNAME
  to `parkingpage.namecheap.com`) first; leftover records at `@` can stop the
  HTTPS certificate from being issued.
- Mail settings should be "No Email Service" unless mail is actually set up.
- To protect against domain takeover, verify `openpower.tools` for the
  `openpower-tools` organisation (organisation Settings > Pages > Add a domain).
  GitHub provides a TXT record at
  `_github-pages-challenge-openpower-tools.openpower.tools`; keep it in place
  permanently. Verifying the apex also covers `www`.
- After the DNS check passes, GitHub requests a Let's Encrypt certificate. It
  can take up to an hour for HTTPS to work and up to 24 hours before "Enforce
  HTTPS" can be switched on. If CAA records are ever added to the zone, one of
  them must allow `letsencrypt.org`.

## Research notes

`docs/research/openpower-landscape.md` collects what was verified about the
OpenPOWER ecosystem (Foundation, Raptor, firmware stack, community, OS and
toolchain ports, naming) with a source URL for every claim and a list of open
questions. `docs/research/stack-verification.md` records the checks behind the
tooling choices above.

## Licence

Code is licensed under GPL-3.0-or-later and content under CC BY-SA 4.0; see
[LICENSE.md](LICENSE.md) for the split, the contribution terms and the
trademark note.
