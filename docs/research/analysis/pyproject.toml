[project]
name = "op-ask-benchmark-analysis"
version = "1.0.0"
description = "Registered analysis for docs/research/benchmark-protocol.md (REGISTERED 2026-09-02, revision 3). Parameters are authoritative in the protocol; scripts must match."
requires-python = ">=3.12"
dependencies = [
    "pymc==6.3.1",
    "arviz==1.3.0",
    "ruptures==1.1.10",
    "numpy",
    "scipy",
]
