<opt:body xmlns:opt="https://www.openpower.tools/ns/opt">
      <p>An embedded A-Frame scene. The vendored runtime (1.3 MB) loads lazily, only on
      pages that contain the element; the entities take their colours from the active
      theme's tokens, and the spinning box is driven by a tick component registered from
      Rust through the same kind of bridge the site's custom elements use. This is the
      seam where interactive hardware walkthroughs will land.</p>
      <h2>Example</h2>
      <opt:scene></opt:scene>
      <h2>Markup</h2>
      <pre>&lt;opt:scene&gt;&lt;/opt:scene&gt;</pre>

</opt:body>
