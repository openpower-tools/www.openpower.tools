[package]
name = "op-pages"
version = "0.1.0"
description = "Page registry and Trunk post_build hook generating the site's pages"
edition.workspace = true
publish.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
op-terms.workspace = true
quick-xml = "0.37"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
op-site = { path = "../op-site" }
