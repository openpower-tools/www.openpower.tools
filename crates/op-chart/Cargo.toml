[package]
name = "op-chart"
description = "Pure chart geometry and classed-SVG emission for openpower.tools: no colour literals, no runtime dependencies"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]

[dev-dependencies]
insta.workspace = true
proptest.workspace = true
