[package]
name = "op-colour"
description = "Colour mathematics for the site's palette tests and fitting: sRGB, OKLab and OKLCH, CIELAB and CIEDE2000, WCAG 2 contrast, APCA, and colour-vision-deficiency simulation"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]

[dev-dependencies]
proptest.workspace = true
